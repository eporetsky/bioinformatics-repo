name: kmersgwas
channels:
  - conda-forge
  - defaults
dependencies:
  - python=2.7
  - r-base
  - r-mass
  - r-matrixcalc
  - wget
  - unzip
  - pip
  - pip:
      # Add any Python dependencies for kmersGWAS if needed
      # - ...

# Note: After creating the environment, run R and execute:
# install.packages('mvnpermute')
