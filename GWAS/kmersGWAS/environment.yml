name: kmersgwas
channels:
  - conda-forge
  - defaults
dependencies:
  - python=2.7
  - r-base
  - r-mass
  - r-matrixcalc
  - wget
  - unzip
  - pip

# Note: After creating the environment, run R and execute:
# install.packages('mvnpermute')
